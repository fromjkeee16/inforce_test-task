# Test Task

## Setup & Run

1. Install dependencies:

```bash
npm i
```

2. Start the React app:

```bash
npm start
```

3. In a separate terminal, start the JSON server:

```bash
npm run server
```
